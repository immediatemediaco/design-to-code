Repository truth:

- Treat the `facts` layer as the source of truth for framework, styling approach, theming model, token system, and reusable component locations. Do not contradict it.
- Do not present guessed project conventions as if they are established repo facts.
- If required implementation detail is missing from the provided facts/context, flag the gap instead of silently making one up.
