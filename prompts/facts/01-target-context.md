Target context:

- Base repository assumptions on the local `./patchwork` checkout mounted into the containers at `/workspace/patchwork`.
- This local design-to-code repo exists to generate and iterate against Patchwork directly. Do not treat the old example Storybook app as the runtime target.
- Component framework: React.
