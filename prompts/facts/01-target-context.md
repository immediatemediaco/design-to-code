Target context:

- Base repository assumptions on `~/Github/__packages/patchwork`, not on this design-to-code sandbox app.
- This local design-to-code repo is an experimentation sandbox for generation workflows. When a prompt asks about the real design-system target, prefer the Patchwork assumptions in this `facts` folder over the sandbox app's implementation details.
- Component framework: React.
