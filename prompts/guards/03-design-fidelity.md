Design fidelity:

- Use semantic HTML elements where appropriate.
- Preserve the design intent from the provided Figma structure and supporting context rather than approximating from memory.
- When the user provides additional instructions, prioritise satisfying them while keeping the component visually close to the original design.
- Match the provided layout structure, hierarchy, and major repeated patterns as closely as the available context allows.
- Treat screenshots as supporting evidence, but anchor implementation decisions in the structured context when the two are in tension.
